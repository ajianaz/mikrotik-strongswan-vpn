# ═══════════════════════════════════════════════════════════════
# VPN Configuration — MANDATORY: fill ALL fields
# ═══════════════════════════════════════════════════════════════
#
# Usage:
#   cp .env.example .env
#   nano .env
#   bash scripts/validate.sh
#   bash scripts/setup.sh
#   bash scripts/deploy.sh
#
# Auth credentials (EAP users, L2TP secrets) are managed via the
# vpn-manager API after deployment:
#   POST /api/v1/tunnels
#
# ═══════════════════════════════════════════════════════════════

# ── Server ────────────────────────────────────────────────────
# Your VPS public IPv4 address (e.g. 152.70.xx.xx)
SERVER_PUBLIC_IP=

# Optional FQDN (e.g. vpn.example.com). Leave empty if unused.
SERVER_FQDN=

# ── Authentication Type ──────────────────────────────────────
# "eap" = IKEv2 EAP-MSCHAPv2 (RouterOS 6.45+, recommended)
# "l2tp" = L2TP/IPsec (RouterOS < 6.45 fallback)
# "both" = enable both protocols
AUTH_TYPE=eap

# ── Rate Limiting (iptables) ─────────────────────────────────
EAP_MAX_LOGIN_ATTEMPTS=5
EAP_LOCKOUT_DURATION_MINUTES=15

# ── Client (MikroTik) ────────────────────────────────────────
# Optional default LAN subnet (per-tunnel override via API)
# CLIENT_LAN_SUBNET=192.168.88.0/24

# ── VPN Pool ──────────────────────────────────────────────────
# Virtual IP pool for VPN clients
VPN_POOL_SUBNET=10.10.10.0/24

# ── Advanced (Optional) ──────────────────────────────────────
# charon log level: 0=none, 1=audit, 2=control, 3=more (default)
# Use 4 for raw debugging, 5 for private key material (DANGEROUS)
# STRONGSWAN_LOGLEVEL=4

# ── PostgreSQL ───────────────────────────────────────────────
# Used by vpn-manager API for tunnel management
POSTGRES_USER=vpnuser
POSTGRES_PASSWORD=

POSTGRES_DB=vpnmgr

# ── VPN Manager API ──────────────────────────────────────────
# API key for authenticating requests to vpn-manager
API_KEY=

# AES-256-GCM encryption key (base64-encoded, 32 bytes) for encrypting
# tunnel passwords at rest. Auto-generated on first run if not set.
# ⚠️ PERSIST THIS VALUE — changing it will make existing encrypted passwords unrecoverable!
# Generate one: python3 -c "import base64,os; print(base64.b64encode(os.urandom(32)).decode())"
ENCRYPTION_KEY=

# HTTP listen address (default: :8080)
# LISTEN=:8080
