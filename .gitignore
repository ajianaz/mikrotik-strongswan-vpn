# Secrets & generated configs (NEVER commit these)
.env
config/conf.d/vpn.conf
config/secret
client/mikrotik.rsc

# Keep config directory structure but ignore generated content
config/conf.d/
config/secret
!config/swanctl.conf

# Logs
logs/

# OS / Editor
.DS_Store
*.swp
*.swo
*~

# Docker
.docker/

# API service secrets
api/.env
postgres-data/
